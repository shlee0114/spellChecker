node {
    stage('Checkout') {
        checkout scm   
    }
    stage('Unit & Integration Tests') { 
        steps {
            script {
                sh "./gradlew clean test --no-daemon"
            }
        }
    }
  }