type GrammarDto{
    errorText : String!,
    fixedText : String
}

type LogDto{
    error : String!,
    fixed : String!,
    count : String,
    fixedTime : String
}

input LogRequest{
    errorText: String!,
    fixedText: String!,
    fixedCount: Int,
    ip: String
}