type GrammarDto{
    errorText : String!,
    fixedText : String
}

input GrammarRequest{
    text : String!
}

input LogRequest{
    errorText: String!,
    fixedText: String!,
    fixedCount: Int,
    ip: String
}