
spring:
  redis:
    port: 6379
    host: 127.0.0.1
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 2
  datasource :
    url : jdbc:postgresql://1.230.255.115:5432/seller_user_table
    username : tlgj255
    password : wntmd1201
  jpa :
    show-sql : true
    database-platform : org.hibernate.dialect.PostgreSQL95Dialect
    database : postgresql

security:
  sessions: always

graphql:
  servlet:
    enabled: true
    corsEnabled: true

server :
  port: 8089
endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false